A) Screen purpose and type:
The screen appears to be a welcome or onboarding screen for a navigation or mapping app, specifically focusing on the feature of saved routes.

B) Layout top → bottom:
- The top section has a black status bar with the text "What's New" in white.
- Below the status bar is a white header with the title "Saved Routes" in bold black text.
- Two bullet points describe the benefits of saving routes: "Save the routes which you use frequently" and "Access the saved routes without adding start and end places again."
- At the bottom of the screen, there is a "GET START" button with a white background and black text.

C) Component inventory (grouped):
- Status Bar: Black with "What's New" in white.
- Header: White with "Saved Routes" in bold black text.
- List Rows: Two bullet points describing the benefits of saved routes.
- Button: "GET START" with a white background and black text.

D) Primary actions + navigation:
- The primary action is the "GET START" button, which likely navigates the user to the main interface of the app.
- There is no footer or bottom navigation present in this screenshot.

• Screen type/purpose: Welcome or onboarding screen for saved routes in a navigation app.
• Key components: Status bar, header, list rows, and "GET START" button.
• Primary CTA(s): "GET START" button.
• Navigation/footer: No footer or bottom navigation present.

STATUS BAR (cropped pass):
1) Left-side notification/app icons:
   - None visible.

2) Right-side system/status icons + time:
   - None visible.
